# Product (MVP)

Product scope lives here: goals, users, features, UI goals, success metrics, epics, and open questions. Implementable stories (`STORY-xx.md`) live in `docs/stories/`.

Add numbered docs when decisions lock. Prefer these files over inventing product behavior.
